caching
  - repos as object in constructor
  - in updateLanguage set repos.currentLanguage to data fetched
  - after user clicked again on language that was checked before, check if data is in the state 

  didMount called
  fetchRepos
  currentLanguage = all
  return repos: all 
  state is set
  re render component

  click on js
  updateLanguage 
  set state to js repos
  set state to js as currentLanguage

reusing card component
  im passing props not whole object
  find common things to display
  what is the shape of player
    score 
    profile
      some common things here

reset button
  show only when battle finished
  clear 


  
